


#branch varibales and task notifications will be different
#notably the notifactions will be differnt... say a task down the tree is replayed again in a different branch, then that task may notify another task before rollback. so taht needs to be accounted for.

ptp_install_dir: '/users/{{ansible_user}}/linuxptp'
cred_dir: "{{module_root}}/files/.aws"
rem_user: dporte7
packages:
  - awscli
  - python3-pip
  - python3-dev
  - htop
  - dstat
  - maven
  - ant
  - virtualenv #required for pip
# - setuptools #required for pip
