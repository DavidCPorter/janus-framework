---
# defaults file for upload_data
_data_upload_dir: '/home/sapauser'
data_upload_dir: '{{ui_data_upload_dir | default(_data_upload_dir)}}'
# collection name is passed in now
_aws_dir_name: "{{ansible_subnet}}/reviews_rf{{replicas}}_s{{shards}}_clustersize{{hosts_ui}}"
aws_dir_name: "{{ui_aws_dir_name | default(_aws_dir_name)}}"
_solr_install_dir: /users/dporte7/solr-8_3
solr_install_dir: '{{ui_solr_install_dir | default(_solr_install_dir)}}'
_solr_home: "{{solr_install_dir}}/solr/server/solr"
solr_home: '{{ui_solr_home | default(_solr_home)}}'
_upload_data_file_path: "{{SAPA_HOME}}/playbooks/roles/upload_data/files"
upload_data_file_path: '{{ui_upload_data_file_path | default(_upload_data_file_path)}}'
_nfs_root: /proj/uic-cs-edu-PG0/exp/solr8/datastore
nfs_root: '{{ui_nfs_root | default(_nfs_root)}}'
_aws_dir: dporter
aws_dir: '{{ui_aws_dir | default(_aws_dir)}}'

# vars for indexing module
_shards: 2
shards: '{{ui_shards | default(_shards)}}'
#replicas is multiplied by #nodes in cluster (e.g. if replicas = 2 and nodes in search engine = 10, then number of replicas == 20)
_replicas: 1
replicas: '{{ui_replicas | default(_replicas)}}'
_collection_name: "{{shards}}_{{replicas}}_index"
collection_name: '{{ui_collection_name | default(_collection_name)}}'
_solrconfig_xml_dest: "{{solr_home}}/configsets/_default/conf/solrconfig.xml"
solrconfig_xml_dest: '{{ui_solrconfig_xml_dest | default(_solrconfig_xml_dest)}}'
_shorter_data: true
shorter_data: '{{ui_shorter_data | default(_shorter_data)}}'

_index_solr:
  - 0_pre_index_config
  - 1_index
  - 2_solr_aws
  - 3_post_index_config