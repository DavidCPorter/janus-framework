---
- name: mkdir for {{oysta_namme}} result data
  file:
    path: "{{local_data_dir}}/{{oysta_name}}"
    state: directory
    mode: '777'

  tags:
    - activate


- name: mkdir for this branches data
  file:
    path: "{{local_data_dir}}/{{oysta_name}}/{{deploy_name}}"
    state: directory
    mode: '777'

  tags:
    - activate


