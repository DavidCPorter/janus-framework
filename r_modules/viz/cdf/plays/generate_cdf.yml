---

- debug:
    msg: python3 generate_figures.py --home_path {{SAPA_HOME}} --oysta_name {{oysta_name}} --query tmp --chart_type {{chart_type}} --module_root {{module_root}}
  tags:
    - activate

- name: generate cdf viz
  command: python3 generate_figures.py --oysta_name {{oysta_name}} --query tmp --chart_type {{chart_type}} --module_root {{module_root}}
  args:
    chdir: "{{module_root}}/files"
  tags:
    - activate
