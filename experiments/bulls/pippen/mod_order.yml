module_order:
  cloud-env:
    aws_tasks.yml: all
    install_tasks.yml: all
  zookeeper:
    configure_tasks.yml: all
    download_tasks.yml: all
    run_tasks.yml: all
  solr:
    config_tasks.yml: all
    install_tasks.yml: all
    run_tasks.yml: all
  elastic:
    install.yml: all
    kibana.yml: all
    metricbeat.yml: all
    run.yml: all
  solr_pipe:
    append_results_to_total.yml: all
    mkdir_data_local.yml: all
    read_results.yml: all
