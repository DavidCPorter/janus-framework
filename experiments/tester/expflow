add modules cloud-env zookeeper solr index_solr http_closed_loop solr_pipe cdf all
order modules cloud-env zookeeper solr index_solr http_closed_loop solr_pipe cdf all
add vars zoo_version 3.5.5 all
add vars solr_version dporter_8_3 solr_heap 1 solr_repo https://github.com/DavidCPorter/lucene-solr.git all
add vars endpoint_host 172.21.0. endpoint_port 8983 all
add vars replicas 2 shards 1 one
add vars replicas 2 shards 2 two
add vars replicas 2 shards 4 three
add vars shorter_data true all
